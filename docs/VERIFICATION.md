# Проверки и приёмка

## Факты до реализации

На 11.09.2026 новый Project Web Pilot состоит из установленного Workflow Kit и подготовленных материалов. Приложение не собрано и не запущено; никаких проверок его Chromium, входа или отправки ещё не было. Профиль DISCOVERY, checks пусты. Проверки документации и workflow не являются тестами будущего приложения.

Исходный Codex Local Mac содержит рабочую реализацию полного recovery packet, ACK/readback и status. В исходных документах зафиксированы 6 context-probe, 5 MCP, 4 HTTP integration и 3 hook-contract проверки на этапе T014–T017. Это результаты исходного проекта, а не запуск этих suite в Project Web Pilot.

## Контрольные эксперименты 11.09.2026

| Проверка | Фактический результат | Вывод |
| --- | --- | --- |
| Новый Work по просьбе восстановить WF001 | Агент показал сообщение; ACK `1153c5ec0a2de8bce3a8a824`, session_id `wf001-work-20260911-mtwn9ffq-gi9ipnle`, source=agent_request | Получение пакета, readback и понятный ответ работают по инструкции |
| Новый Work с запретом самостоятельного восстановления | Агент сообщил, что стартовый пакет от hook не получен и самостоятельное восстановление не выполнялось | Автоматическая стартовая доставка через проверявшееся подключение не подтверждена |
| MCP после перезапуска | HTTP initialize и tools/list подтвердили 50 tools, включая четыре context tools | Сервер готов; это не установка hooks у Web Work |
| Личный плагин в Safari | UI позволял Refresh, но не показывал загрузку hook bundle | Для просмотренного подключения не найден способ включить внутренний hook; Chromium-оболочка решает другую часть — отправку сообщения |

В положительном эксперименте факты были WF001, plan_revision=40, scope=macos-handoff-001, ACTIVE/READY_FOR_ACCEPTANCE. Это исторический снимок: во всех новых опытах получать актуальные значения, не подставлять revision=40 или прежние session/probe IDs.

Источник результатов: [исходный CONTEXT_PROBE.md](</Users/oleksandroliinyk/VSCODE/Codex Local Mac/docs/CONTEXT_PROBE.md>). Задачи исходного чата: «Восстановление контекста проекта» `6aa3afc4-2580-83ed-a5f6-eeb9bd5b6da4` и «Проверка стартового пакета» `6aa3b152-f46c-83eb-9f2e-463a12fbbc94`. Для продолжения достаточно этого описания и исходного документа; доступа к истории чатов не требуется.

## Проверка подготовленного workspace

Проверить через штатные validate, status и recover: согласованность плана с коммитами, отсутствие текущей задачи и незавершённой транзакции, следующий шаг T004, полноту COMPLETE и включение опорного контекста. Проверить UTF-8 документов и существование локальных ссылок. Отдельно убедиться, что HEAD и рабочие деревья WF001/Codex Local Mac не изменились при подготовке.

Context pack включает компактный раздел WORKFLOW_START и каноническую стартовую последовательность; остальные подробности доступны через каталог. Бюджет пакета настроен на soft 16000 / hard 32000 оценочных токенов и 65536 UTF-8 байт, чтобы вместить обязательные инструкции и связанный коммит. Превышение не обходить усечением обязательных данных: уменьшать конкретные разделы или дробить задачи.

## Настройка функциональных проверок

T004 сначала документирует точный стек и реальные команды и завершается отдельным документальным коммитом. Между T004 и T005 применить полную конфигурацию через config:apply, сохранив budget и необходимые поля. Затем через plan:apply назначить ID проверок будущим задачам. Не добавлять заглушку, которая всегда завершается успешно. Первая проверка каркаса может проверять загрузку/синтаксис фактически созданных файлов; по мере появления модулей подключать проверки их поведения.

Нужны осмысленные проверки границ: папка/чат, принадлежность процесса, недоступность службы, сериализация отправки, неопределённый исход Send, чужой или устаревший ACK. Локальный fixture может воспроизводить черновик и изменение поля, но не заменяет опыт на настоящем ChatGPT. Команды и новые файлы тестов уточнять до начала соответствующей задачи; плановые имена не означают наличие тестов сейчас.

## Реальная проверка прототипа

1. Запустить новое приложение на Mac пользователя. Видны собственный сайдбар и реальный ChatGPT в одном окне; пользователь проходит вход доступным штатным способом.
2. Выбрать подготовленный тестовый workspace. Для первого read-only опыта допустим WF001; путь записать явно, заранее сохранить его Git-состояние.
3. Приложение проверяет и при необходимости запускает существующие MCP/tunnel. Веб-чат действительно видит необходимые tools.
4. Приложение само отправляет ровно одно стартовое сообщение без копирования пользователем. В нём правильные workspace и новые идентификаторы.
5. Агент получает полный пакет, подтверждает его и показывает user_message. Сайдбар получает соответствующий status и показывает тот же проект и версию плана.
6. Сохранить небольшой отчёт: версии приложения/Electron, путь и project_id, chat URL/ID, session_id, request_id, probe_id, полученные факты, время и видимый результат. Не сохранять challenge, cookies, ключи или полный приватный контекст без необходимости.
7. Сравнить Git тестового workspace до/после. Первый сценарий не меняет его файлы.

Только после этого можно считать основную цепочку прототипа проверенной. Пользователь подтверждает физический результат. Если вход, плагин или поле недоступны, сохранить точную причину и наблюдение; не объявлять успех по одному рендеру окна.

## Повторный запуск и ошибки

Проверить сохранённый вход и привязку чата, повторное открытие без дублирующего сообщения, другой workspace во время работы чата, существующий черновик, продолжающуюся генерацию, пропавший MCP tool, неготовый tunnel, таймаут Send с неизвестным исходом, изменившийся план, неправильный session_id и старое подтверждение. Автоматический повтор не должен создавать второй пользовательский ход при неизвестном результате первого.

Закрытие окна не останавливает общие службы других чатов. Подключение существующей папки и её план в будущих этапах не должны сбрасываться. Встроенный браузер не заменяется внешним как неявный способ засчитать проверку.

## Что не считается приёмкой

- Наличие hooks.json, новый plugin metadata или ручной workflow_context_hook.
- ACK из прежнего теста или подтверждение, отправленное самой оболочкой вместо агента.
- Рабочий MCP в Safari как доказательство входа и работы встроенного Chromium.
- Успешная сборка как доказательство пользовательского сценария.
- Пауза, reconnect, счётчик сообщений или новый пакет как доказательство auto compact.
- Кросс-сборка как доказательство запуска на Windows.

## T004 — подготовка разработки

Зафиксированы точные зависимости и полная DEVELOPMENT-конфигурация в ARCHITECTURE. В этой задаче проверены доступность версий, требования Node, исходные control.py/status и recover/ACK/status. Исходники имеют прежние HEAD: WF001 20260a0, Codex Local Mac 1d0ddef; рабочие деревья чистые. Приложение ещё не собрано; ни одна будущая suite не объявлена пройденной. После документального commit применяются config:apply и verification_ids.

## T005 — проверка каркаса

11.09.2026: npm install завершён, audit: 0 vulnerabilities. node --check src/main.mjs пройден. Electron 44.3.0 --shell-smoke действительно запустил два WebContentsView и вернул require=undefined, process=undefined; exit code 0. При первом запуске найдена и исправлена блокировка ESM-загрузки ожиданием app.whenReady на верхнем уровне. Вход и полный сценарий ChatGPT этой проверкой не подтверждены.

## T006 — связи папок и чатов

Пройдены 5 Node tests: пробелы/кириллица и symlink, сохранение после перезапуска, разделение двух проектов и чужих ACK, смена project_id, повреждённый файл настроек и неприемлемые URL. Локальная разметка сайдбара готова к интеграции T010. Через Computer Use увидена настоящая форма входа ChatGPT в окне каркаса; вход аккаунта пока не подтверждён.

Для recovery после lockfile в T005 hard budget изменён управляемой конфигурацией до 131072 байт / 65536 оценочных токенов; прямые зависимости читаются полностью. Это изменение вместимости пакета, а не усечение проверок.

## T007 — подключение runtime

Пройдены 5 Node tests: повторное использование готовых служб, один start при необходимости, запрет действий с чужим процессом, отсутствие tunnel, SSE/JSON/session headers, разрешение только status и отсутствие обязательных tools. Исправлена тестовая нормализация /var → /private/var на macOS.

Живой McpRuntime.ensure подтвердил mcpReady=true, tunnelReady=true, serverName=Codex Local Mac, toolCount=50, protocolVersion=2025-03-26. Context status для WF001 с новым transport-test session_id вернул latest=null и правильную папку. Recover и ACK в этой проверке не вызывались. Готовность плагина внутри нового входа ChatGPT остаётся отдельной проверкой.

## T008 — доставка сообщения

Пройдены 7 jsdom/Node tests: один Send и наблюдаемый user message, неизменность чужого черновика, активная генерация, правка текста пользователем перед click, неопределённый результат без повторного Send, сериализация/отмена, исключение assistant quote, login и contenteditable-ввод. Fixture воспроизводит обычное поле; текущая разметка настоящего ChatGPT после входа ещё требует проверки. Успех fixture не считается реальным отправленным сообщением.

## T009 — подтверждение контекста

Пройдены 7 Node tests: свежий ACK и полный набор фактов, чужая папка/сессия/старый probe, изменившийся план, подмена подтверждения, точный стартовый prompt, durable state до Send, повторное открытие без сообщения, uncertain outcome без повторной отправки, baseline старого ACK и отмена до отправки. Исправлен fixture, ошибочно возвращавший runtime session fields вместо одних фактов проекта. Никакой настоящий ACK этой suite не создаётся; реальная проверка ждёт входа в ChatGPT.

## T010 — интеграция и локальная сборка

11.09.2026: npm test — 26 тестов пройдены; npm run smoke — пройден интеграционный сценарий в Electron 44.3.0 / Chromium 152.0.7977.78. Он проверяет два view, ограниченный IPC, отсутствие Node/bridge у удалённой страницы, один стартовый текст, повторное открытие без дубля и новый session_id после кнопки нового чата. Вывод явно содержит liveChatGPT=false и realAck=false. npm run build собрал локальное arm64-приложение.

В настоящем окне пользователь вошёл в ChatGPT; повторный запуск сохранил вход. Выбрана папка WF001, локальные службы готовы. Оболочка сама заполнила поле Work, затем контроль текста выявил абзацные переносы; адаптер исправлен и покрыт регрессионной проверкой. В 09:12:16 UTC приложение само отправило сообщение, Work начал ответ, постоянная связь с новым чатом сохранилась. Реальная цепочка recover/ACK проверяется отдельно в T011. Полноценная пользовательская приёмка ещё не выполнена.

## T011 — реальная вертикальная проверка, 11.09.2026

Проверена упакованная Project Web Pilot 0.1.0 (Electron 44.3.0, Chromium 152.0.7977.78) на этом Mac. Вход выполнен пользователем в собственном профиле приложения. Автоматическая отправка состоялась в 09:12:16 UTC, реальный агент завершил recover/ACK в 09:12:58 UTC. Нажатия Send вручную не было. Ответ Work содержит возвращённое user_message, а сайдбар показывает «Контекст подтверждён».

| Факт | Значение |
| --- | --- |
| Workspace | /Users/oleksandroliinyk/VSCODE/WF001 |
| project_id | 44f12be2-f5af-4f4a-a435-54ac4541b9f9 |
| Чат | https://chatgpt.com/c/6aa3c5f0-5e50-83eb-a96c-e9c1af2f60ca |
| session_id | web-pilot-bfad0a4d-680a-4a9e-8879-a320d6504f4e |
| request_id | wp-request-84e48b01-fe5b-4868-9660-f8c7d3320750 |
| probe_id | 4a8cb1db8ad1423b64870259 |
| Факты ACK | WF001; plan_revision=40; scope=macos-handoff-001; ACTIVE / READY_FOR_ACCEPTANCE; task_id=null; task_title=null |
| Публичный status | latest.acknowledged=true; last_receipt.status=acknowledged; source=agent_request; та же папка/сессия/probe |
| Ограничение события | runtime_event_verified=false; last_compaction=null |

Оболочка получила подтверждение только через workflow_context_status; recover/ACK вызвал реальный агент. Полный публичный status и скриншот сохранены локально в ignored .harness/runtime/evidence/real-startup.json и real-startup.png. В них нет challenge, cookies или ключей. Совпадение фактов проверено независимо от текста ответа.

WF001 после опыта: HEAD 20260a0cebd825c1d0415070d9eca6e1b1585fd4, чистое рабочее дерево. Codex Local Mac: HEAD 1d0ddef03c6d6af0dd3167831a20296307e2d930, чистое рабочее дерево. Исходные файлы не изменены. Результат подтверждает один реальный запуск выбранного аккаунта/режима Work; устойчивость при изменениях сайта, внутренний SessionStart и auto compact не подтверждены. Пользовательская приёмка самого прототипа ожидается.
