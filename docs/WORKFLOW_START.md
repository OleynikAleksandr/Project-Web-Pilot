# Начало работы

## Опорный контекст новой сессии

Рабочая папка — `/Users/oleksandroliinyk/VSCODE/Project Web Pilot`, project_id `cf944136-d1fc-4bd5-9ea0-e46d1fe230e7`. Название пользователя — Project Web Pilot. Не возобновлять разработку WF001 или Codex Local Mac вместо этого проекта.

Продукт: одно локальное окно, слева сайдбар проектов, справа настоящий ChatGPT Web во встроенном Chromium с собственным входом. Полная цель включает создание/подключение workspace и планы. Первый прототип уже реализует **выбор подготовленной папки → готовые MCP/tunnel → автоматическое видимое сообщение → MCP recovery → ACK → ответ агента и состояние сайдбара**.

11.09.2026 пользователь поручил продолжать до первого запускаемого прототипа для тестирования и фидбэка. Реализация разрешена этим поручением и выполнена. Авторитетные статусы задач, текущую revision и коммиты читать из `.harness/plans/todo-plan.md` и workflow status. Следующий продуктовый объём определяет отзыв пользователя; одно восстановление контекста не разрешает создавать новый scope.

Реальная проверка пройдена в упакованном приложении на WF001: вход, автоматическая отправка, ACK 4a8cb1db8ad1423b64870259 и зелёный статус. Повторное открытие сохранило чат без нового Send. Подробности — VERIFICATION. Пользовательская приёмка ожидается; scope не архивирован.

Стек: Electron 44.3.0/WebContentsView, JavaScript ESM, @electron/packager 20.3.0, DEVELOPMENT. Проверки назначены через config:apply и plan:apply; 28 Node tests и Electron smoke пройдены. Локальное приложение: `.harness/runtime/build/Project Web Pilot-darwin-arm64/Project Web Pilot.app`. Запуск и ограничения — README. Профиль/связи находятся в Library/Application Support/Project Web Pilot, вне Git.

Исходники для чтения: `/Users/oleksandroliinyk/VSCODE/WF001` (Workflow Kit 1.1.0) и `/Users/oleksandroliinyk/VSCODE/Codex Local Mac` (MCP, control.py, context recover/ACK/status). Их не изменять ради нового приложения. Конкретные пути и SHA — SOURCE_WORKSPACES. Прототип использует существующий установленный MCP/tunnel; создание новых workspace и самостоятельная поставка runtime ещё не выполнены.

Каноническая последовательность — CONTEXT_DELIVERY. Стартовое сообщение является обычным пользовательским вводом оболочки; MCP запускает приложение до обращения агента. Внутренние hooks Web Work не подключены. Compact нельзя считать обнаруженным по перезагрузке, паузе или ACK. Переключение папки не перенаправляет работающий чат. Модельных API-вызовов нет. Подтверждение сверяется с публичным status; оболочка никогда не вызывает recover/ACK/hook вместо агента.

## Порядок восстановления

1. Использовать этот workspace и актуальные AGENTS/packet. Если packet отсутствует, выполнить `./scripts/workflow recover --format text`; проверить COMPLETE, project_id и workspace.
2. Прочитать `./scripts/workflow status`: текущую задачу, Git и незавершённую транзакцию. Исторический отчёт не заменяет состояние плана.
3. Прочитать нужные документы через DOCUMENTATION_INDEX: PRODUCT, DECISIONS, ARCHITECTURE, CONTEXT_DELIVERY, SOURCE_WORKSPACES, VERIFICATION.
4. Продолжать изменения по поручению пользователя и единственному плану. Для нового отзыва сначала определить конкретные изменения, включить пути через plan:apply и начать задачу через task:start.
5. Код, проверки, связанные документы и статус фиксировать вместе через workflow commit. Не обходить hooks и не редактировать машинные статусы вручную.

Только если настоящий hook доставил DELIVERY-MARKER в дополнительном контексте, подтвердить его штатным hook:ack с фактическим клиентом. Не искать маркер в журнале ради отметки успешности. Обычный recover подтверждает пакет, а не доставку hook.

## Две разные сессии

Локальная сессия Codex разрабатывает Project Web Pilot с Workflow Kit этой папки. Встроенный ChatGPT получает отдельное стартовое сообщение и работает с выбранной папкой через MCP. Успех одного механизма не подтверждает внутренние hooks другого.

## Служебные команды

```bash
./scripts/workflow status
./scripts/workflow recover --format text
./scripts/workflow validate
./scripts/workflow doctor
```

В Windows установленный Workflow Kit использует `./scripts/workflow.cmd` в PowerShell/CMD. Это переносимость комплекта, а не проверенная Windows-версия приложения. Повторная установка в эту папку не требуется. Приёмка и передача в новую сессию не архивируют scope.
