# Начало работы

Project Web Pilot использует адресованный recovery Workflow Kit: пакет принадлежит sessionId и planId выбранной сессии. Прочитайте доставленный контекст и кратко подтвердите восстановление. Для первого ответа повторно запрашивать пакет не нужно.

Если пакета нет, используйте `./scripts/workflow recover --session <sessionId> --format text`. Во всех дальнейших командах сохраняйте адрес из своего пакета; выбранный в UI чат не меняет владельца команд.

1. При NONE обсудите следующий этап по OVERVIEW, MODULES и DOCUMENTATION_INDEX.
2. Сопоставьте запрос с существующей частью проекта и её контрактом. При отсутствии контракта сначала согласуйте specification/planning document.
3. Создайте собственный план через scope:create либо продолжите его через plan:apply. Завершённые задачи и commit references сохраняются.
4. Перед изменениями выполните task:start; каждую микрозадачу завершите управляемым commit с применимыми проверками. Одновременно пишет один агент.
5. Единственная последняя DOCS актуализирует весь комплект документов. Выполненный план остаётся в сессии до прямой команды на archive.
6. Будущий план готовится через plan:prepare; пользователь вручную создаёт сессию через Chat/Work. Автоматического перехода после DOCS нет.

## Состояние проекта — 18.09.2026

Текущая поставка — **0.6.40**, Workflow Kit **1.4.1**, macOS arm64 и Windows x64. При первом создании проекта расположение выбирается явно перед именем и сохраняется для следующих проектов и перезапусков. Chat/Work применяет preview и открывает первую сессию одним действием. Подробности — docs/WORKSPACE_SETUP.md.

Пользователь принял чистый запуск macOS 0.6.38 и проверку 0.6.39. Новая пользовательская проверка 0.6.40, Windows 11 ARM64 с x64-эмуляцией и нативная Windows x64 не объявляются выполненными. Исходники, установленные файлы и архивы 0.6.40 сверены; доказательства — docs/VERIFICATION.md и docs/RELEASE.md.

18.09.2026 пользователь прямо поручил закрыть **first-run-onboarding-031** сессии web-pilot-ee60a2b3-bdde-4d7f-83cd-a4e25767fcbe и обновить GitHub. Оставшиеся задачи завершают фиксацию результатов и передачу невыполненных испытаний; условия для следующего агента — docs/TRANSFER_TO_WINDOWS.md. После финальной DOCS план архивируется штатно; эта сессия получает NONE без создания нового плана или сессии.

Исправления 0.6.38–0.6.40 выполнялись в **first-run-corrections-032** сессии 01a0b318-cc66-7743-9de7-696f05fff6e6. Этот план не закрывается и не переадресуется данным поручением. История испытаний и разрешений Computer Use — docs/CLEAN_INSTALL.md.

## Разработка и выпуск

Общий source macOS/Windows — [Project-Web-Pilot](https://github.com/OleynikAleksandr/Project-Web-Pilot). Runtime, userData, ключи и платформенные сборки не переносятся в Git. Встроенный Chromium показывает ChatGPT Web; локальные действия доступны через существующие MCP/tunnel, модельных API нет.

Для изменений приложения и выпуска обязательны Node suite и Electron smoke на изолированных fixtures. Документальные задачи выполняют назначенные проверки Workflow Kit; готовый проверенный бинарный релиз не пересобирается без изменения приложения. Реальные чаты, профили и гостевые проекты не используются для разрушительных проверок.

`npm run build:mac` обновляет постоянный `Project Web Pilot.app` в корне workspace с сохранением Finder-алиаса и создаёт отдельный ZIP. Поставки 0.6.40 находятся в `~/Downloads/WebPilot-0.6.40/`. Обновление работающего приложения применяется после полного выхода и повторного запуска. Git push публикует исходники и документацию, не заменяет выпуск ZIP.
