# Решения проекта

Источник решений — обсуждение с пользователем 11.09.2026 в WF001. Этот документ сохраняет договорённости для следующей сессии; актуальные статусы задач принадлежат только `.harness/plans/todo-plan.md`.

## Подтверждено пользователем

| Решение | Значение для реализации |
| --- | --- |
| Создать третий проект на основе WF001 и Codex Local Mac | Новый самостоятельный репозиторий, исходные проекты сохраняются |
| Встроенный Chromium в одном окне приложения | Слева собственный сайдбар, справа настоящий ChatGPT; не внешний браузер |
| Сайдбар создаёт и подключает workspace | Переиспользовать установку и восстановление Workflow Kit |
| Приложение само вставляет и отправляет текст в поле пользователя | Собственный локальный стартовый сценарий, видимое обычное сообщение |
| Передавать workspace и первоначальную задачу | У агента есть точная папка и понятная цель восстановления |
| Для проверки подходят Chat или Work | Проверки текущего MCP проходили в Work; для прототипа разумно начать с него |
| Папка создана пользователем через Project Workflow Kit | `/Users/oleksandroliinyk/VSCODE/Project Web Pilot` — целевой workspace |
| Подготовить материалы и первичный план для новой сессии | В текущем сеансе документальная подготовка, без кода приложения |

## Предлагаемое техническое направление

Electron с WebContentsView — обоснованный кандидат для собственного Chromium и переносимого интерфейса. В T004 выбраны Electron 44.3.0 и @electron/packager 20.3.0; полный профиль записан в ARCHITECTURE. Смена продуктового решения о встроенном браузере требует обсуждения.

Для первого прототипа приложение само запускает существующий MCP и tunnel до отправки сообщения. Первое сообщение просит агента получить пакет через уже реализованные MCP-инструменты, а не содержит вручную скопированную устаревающую версию всего контекста. Сайдбар получает подтверждение из status. Это проектное решение на основе обсуждённого сценария.

Первый этап — macOS; модель вызывается веб-сервисом ChatGPT. Полный перенос двух кодовых баз и Windows-поставка откладываются до проверки основного сценария.

## Не подтверждено

- Вход конкретного аккаунта ChatGPT в выбранной сборке встроенного Chromium.
- Возможность этого входа использовать существующий MCP-плагин без дополнительных действий пользователя.
- Устойчивость адаптера поля ввода ChatGPT при изменениях страницы.
- Получение достоверного события автоматического compact из Web Work.
- Автоматическая отправка контекста перед каждым пользовательским сообщением.
- Перенос контекстных инструментов на Windows: в исходном Windows-комплекте нет macOS-модуля context_probe.py.

## Границы полномочий текущей подготовки

Не начинать функциональные задачи T004–T013 в этой сессии. Следующая сессия сначала восстанавливает контекст и действует по новому поручению пользователя. Подготовленный план не является командой немедленно выполнить его весь. Не создавать дополнительные пользовательские задачи, не менять глобальные инструкции, trust, MCP-плагин или исходные репозитории ради передачи документации.

Приёмка прототипа не является архивированием scope. Публикация, выпуск установочного пакета и изменения исходных проектов требуют соответствующего поручения.

## Поручение новой сессии 11.09.2026

Пользователь: «Продолжай и не останавливайся вплоть до сборки первого прототипа в котором я смогу что-то уже протестировать и дать тебе фидбэк». Это разрешает последовательную реализацию T004 и следующих задач до доступного для проверки приложения. Ограничение предыдущей документальной сессии больше не блокирует эти задачи. Прототип собирается локально на macOS arm64; вход и физическая приёмка остаются за пользователем. Исходные репозитории, глобальные настройки и секреты не меняются.
