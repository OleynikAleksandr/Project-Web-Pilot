
<!-- workflow-kit:begin -->
# Project Workflow Kit

Общайся по-русски. Канонический протокол работы: `.harness/kit/WORKFLOW.md`, раздел «Обязательные правила» передаётся также через SessionStart.

Единственный активный план — `.harness/plans/todo-plan.md`; каталог документов — `docs/DOCUMENTATION_INDEX.md`. При новом старте, возобновлении и компакте используй текущий recovery packet. Если packet отсутствует, выполни `./scripts/workflow recover --format text` до реализации.

Scope NONE разрешает обсуждение идеи и первого плана. Не выбирай стек до необходимости. Статусы меняются командами workflow. Каждая задача завершается отдельным управляемым commit вместе с документацией. Не обходи Git hooks. Релиз, публикация и архивирование scope выполняются только по указанию пользователя; приёмка не равна закрытию.

В Windows команды имеют вид `./scripts/workflow.cmd status` (PowerShell/CMD); в Git Bash доступен `./scripts/workflow`. Пути внутри JSON плана всегда записываются через `/`.
<!-- workflow-kit:end -->

# Project Web Pilot — границы разработки

- Имя проекта выбрано пользователем: Project Web Pilot. Рабочий корень — `/Users/oleksandroliinyk/VSCODE/Project Web Pilot`.
- Идея уже обсуждена. Сначала прочитай `docs/WORKFLOW_START.md`, `docs/PRODUCT.md`, `docs/DECISIONS.md` и текущий recovery packet. Не предлагай заново внешний Chrome: пользователь выбрал встроенный Chromium в одном окне со своим левым сайдбаром.
- Документальная подготовка T001–T003 завершена. 11.09.2026 пользователь новой сессии прямо поручил продолжать до сборки первого тестируемого прототипа. Реализация T004 и последующих задач авторизована; не запрашивать её повторно. Текущие статусы читать из плана. При одном восстановлении контекста в будущей сессии не придумывать новый scope.
- Ближайший результат — проверочный macOS-прототип: собственная оболочка запускает MCP, отправляет стартовое сообщение в реальный ChatGPT Web и получает подтверждение контекста. Канонический контракт — `docs/CONTEXT_DELIVERY.md`; архитектура — `docs/architecture/ARCHITECTURE.md`.
- Модель работает через веб-аккаунт ChatGPT. Не добавляй модельные API-вызовы и второго кодирующего агента. Транспортный tunnel и локальный MCP — существующая основа.
- Источники для чтения: `/Users/oleksandroliinyk/VSCODE/WF001` и `/Users/oleksandroliinyk/VSCODE/Codex Local Mac`. Точные файлы и SHA — `docs/SOURCE_WORKSPACES.md`. Не менять источники при реализации третьего проекта без соответствующего поручения; не переносить ключи, browser profiles и runtime-состояние.
- Переключение workspace не меняет cwd уже работающего чата. Все локальные действия привязаны к явной папке. Собственный стартовый хук оболочки не выдавать за внутренний SessionStart или auto compact ChatGPT.
- Успех определяют реальный вход, отправка сообщения, вызовы MCP, ACK и видимый результат в приложении. Сборка, HTTP и ручной hook отдельно не являются пользовательской приёмкой. ACK не подделывать и не получать challenge из файлов журнала.
- До функциональных задач завершить T004, затем между задачами применить профиль DEVELOPMENT и реальные проверки через config:apply; назначить verification_ids через plan:apply. T004 определяет полный профиль; после его завершения применять DEVELOPMENT до T005. Пустые либо не запущенные checks не означают успешной проверки.
- После каждого этапа обновлять связанные документы и единственный активный план управляемыми командами. Не создавать параллельный TODO.md. Новые пути сначала включать в план. Подготовка завершена — не значит, что готово приложение или проверены его hooks.
