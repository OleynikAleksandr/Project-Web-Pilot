
<!-- workflow-kit:begin -->
# Project Workflow Kit

Общайся по-русски. При старте/возобновлении/compact используй доставленный recovery capsule; если его нет — `./scripts/workflow recover --session <sessionId> --format text`. Во всех командах указывай sessionId из своего пакета; для чтения/подготовки другого плана также явно указывай planId. Выбор чата в интерфейсе не меняет владельца команд агента.

Workflow Kit ведёт проекты любого типа. Каждая сессия имеет собственный сохраняемый план либо NONE. Канонические планы находятся в `.harness/plans/by-id/`; прежний `.harness/plans/todo-plan.md` сохраняется для совместимости и истории. Не выбирай план по глобальному последнему указателю. Старый CLI без адреса допустим лишь до появления привязанных/подготовленных планов. При NONE используй `docs/architecture/OVERVIEW.md`, `docs/MODULES.md` и `docs/DOCUMENTATION_INDEX.md` и обсуждай следующий этап существующего проекта.

Новый запрос сначала сопоставь с частью проекта в `docs/MODULES.md`; если её нет — сначала создай и согласуй specification/planning document. Для программного модуля фиксируй facade, входы/выходы, границы и инварианты; внутреннюю реализацию дроби на узкие классы/микроклассы. План реализует согласованный контракт.

Каждый рабочий план сохраняет три required-ссылки навигации и заканчивается задачей DOCS — «Актуализация всех документов проекта». После её commit сообщи результат пользователю. Выполненный план остаётся в сессии: новое поручение добавляется через plan:apply, прежние DONE-задачи/коммиты сохраняются, финальная DOCS открывается повторно. Обязательной кнопки приёмки нет.

Будущий план готовь через plan:prepare из текущей сессии. Это отдельный канонический план; свой план не заменяй. Пользователь создаёт сессию с ним через существующий выбор Chat/Work; отмена сохраняет черновик. Исходная сессия сохраняет ссылку на тот же план. Меню проекта создаёт самостоятельные Chat/Work с NONE.

Machine-managed поля меняются только командами Workflow Kit. Перед правкой начни task; каждую микрозадачу заверши workflow commit с проверками. Один пишущий агент на worktree, ожидаемая revision и транзакционные блокировки обязательны. Не обходи hooks, не удаляй/не откатывай посторонние изменения. Архивирование требует прямого поручения пользователя и не создаёт следующего scope или сессии автоматически.

Большой `.harness/kit/WORKFLOW.md` — справочник команд, repair и установки. Дополнительные документы бери через план/spec и индекс. В Windows PowerShell/CMD используй `./scripts/workflow.cmd`; в Git Bash/macOS — `./scripts/workflow`.
<!-- workflow-kit:end -->

# Project Web Pilot — границы разработки

- Имя проекта выбрано пользователем: Project Web Pilot. Рабочий корень — `/Users/oleksandroliinyk/VSCODE/Project Web Pilot`.
- Идея уже обсуждена. Сначала прочитай `docs/WORKFLOW_START.md`, `docs/PRODUCT.md`, `docs/DECISIONS.md` и текущий recovery packet. Не предлагай заново внешний Chrome: пользователь выбрал встроенный Chromium в одном окне со своим левым сайдбаром.
- Документальная подготовка T001–T003 завершена. 11.09.2026 пользователь новой сессии прямо поручил продолжать до сборки первого тестируемого прототипа. Реализация T004 и последующих задач авторизована; не запрашивать её повторно. Текущие статусы читать из плана. При одном восстановлении контекста в будущей сессии не придумывать новый scope.
- Версия 0.1 проверена на WF001. По новому поручению T014–T017 полный контекст передаёт приложение первым сообщением; агент кратко подтверждает восстановление и описывает проект без обязательных recover/ACK вызовов. Контракт обновлён в docs/CONTEXT_DELIVERY.md. Канонический контракт — `docs/CONTEXT_DELIVERY.md`; архитектура — `docs/architecture/ARCHITECTURE.md`.
- По поручению T018–T020 workspace хранит список сессий. Двойной клик раскрывает дерево, дочерняя строка открывает конкретный чат; новый чат добавляется без замены прежних. Формат хранения мигрирует с резервной копией.
- Модель работает через веб-аккаунт ChatGPT. Не добавляй модельные API-вызовы и второго кодирующего агента. Транспортный tunnel и локальный MCP — существующая основа.
- Источники для чтения: `/Users/oleksandroliinyk/VSCODE/WF001` и `/Users/oleksandroliinyk/VSCODE/Codex Local Mac`. Точные файлы и SHA — `docs/SOURCE_WORKSPACES.md`. Пользователь разрешил изменить Codex Local Mac для очистки hook/ACK диагностики и прямой передачи контекста; WF001 не менять; не переносить ключи, browser profiles и runtime-состояние.
- Переключение workspace не меняет cwd уже работающего чата. Все локальные действия привязаны к явной папке. Новая сессия или явное обновление инициирует доставку полного пакета приложением; просмотр сохранённого чата не запускает новую подготовку или отправку; не добавлять экспериментальные проверки hooks в пользовательский ответ.
- Успех определяют полный пакет в реальном первом сообщении и короткий ответ агента о выбранном проекте. Сайдбар подтверждает передачу сообщения. Машинный ACK не используется; сборка и HTTP отдельно не являются пользовательской приёмкой.
- Профиль DEVELOPMENT и verification_ids уже настроены управляемо. Реальные проверки: актуальный Node suite и Electron smoke; smoke использует TEST FIXTURE; реальный первый ответ проверяется отдельно. Проверять назначенные команды перед commit; пустые либо не запущенные checks не означают успех.
- После каждого этапа обновлять связанные документы и собственный план сессии управляемыми командами. Не создавать параллельный TODO.md. Новые пути сначала включать в план. Сборка и локальные проверки не заменяют пользовательский результат.

- Создание нового workspace и подключение существующего разрешены пользователем и реализованы через встроенный исходный Workflow Kit. Канонический контракт — `docs/WORKSPACE_SETUP.md`; перед новым изменением этого поведения прочитать его. Исходный WF001 остаётся отдельным проектом только для чтения.
- Архивирование workspace, возврат и удаление локальной папки реализованы в T025–T028 по поручению пользователя. Канонический контракт — `docs/PROJECT_ARCHIVE.md`. Архив приложения не меняет execution_scope_status. Облачные чаты удалять не разрешено. Физическое удаление реального проекта требует его явного выбора и подтверждения в интерфейсе; автоматические проверки удаляют только собственные временные fixtures.

- В 0.6.20 добавлен автономный Доктор проекта: `docs/modules/project-doctor.md`. 16.09.2026 пользователь реально запустил его на текущем проекте; `.harness/kit-manifest.json` успешно reconciled до Workflow Kit 1.3.0. Намеренно возвращать stale-состояние нельзя; дальнейшие сценарии повреждения проверяются только на временных fixtures.
- Обязательный порядок каждого macOS-релиза — `docs/RELEASE.md`: `npm run build:mac` должен обновлять `/Users/oleksandroliinyk/VSCODE/Project Web Pilot/Project Web Pilot.app` в корне проекта и отдельно создавать версионированный ZIP. Сохраняй filesystem identity корневой папки `.app`, чтобы Finder-алиас переживал обновления. Не объявляй релиз готовым по одному ZIP: проверь версию установленного app и соответствие staging. Путь запуска не переносить в новую папку версии. App в корне исключён из Git и обеих упаковок.

## Завершённый этап — быстрое открытие сессий

Scope session-opening-performance-029 реализован в 0.6.29 / Workflow Kit 1.4.1. Контракт — docs/modules/session-opening-performance.md; проверки и пределы измерений — docs/VERIFICATION.md. Все изменения находятся в main, лишние worktrees/временная ветка удалены с backup. План принадлежит сессии из доставленного recovery; команды явно адресуются её --session. Завершение сохраняет план и prepared link, не архивирует его и не создаёт сессию. Новое поручение продолжается через plan:apply с повторной DOCS; archive только по отдельной прямой команде.


## Текущий выпуск — подключение и разрешения MCP / 0.6.45

19.09.2026 выпущена **0.6.45** для macOS arm64 и Windows x64 одной командой `npm run build`; ZIP находятся в ~/Downloads/WebPilot-0.6.45/. В общем мастере обеих платформ есть отдельный шаг подключения MCP к ChatGPT и видимый блок «Разрешения MCP»: путь к настройкам, четыре режима и добровольный выбор Allow all actions для работы без повторных подтверждений. На новом Mac подключение также нужно создать; оно не появляется автоматически. Создание проекта не запрашивает автора и email. Исходники, установленное приложение и обе упаковки сверены. Пользователь считает план завершённым с этим дополнением; агент не запускает VM и не использует Computer Use.

Контракт — docs/modules/first-run-onboarding.md, реестр — docs/CLEAN_INSTALL.md. Планы 031 и 032 принадлежат разным сессиям; адрес каждой команды берётся из доставленного recovery, а не из этой общей инструкции. Последнее поручение: проверки выполняет пользователь, агент не использует Computer Use и не запускает VM. Новый чистый проход не объявлять завершённым по fixture, сборке или компиляции диалогов.

18.09.2026 пользователь поручил закрыть план first-run-onboarding-031 и передал испытания Windows 11 другому агенту. Итог macOS и границы доказательств — docs/CLEAN_INSTALL.md; условия и оставшиеся критерии Windows — docs/TRANSFER_TO_WINDOWS.md. План first-run-corrections-032 принадлежит другой сессии и этим закрытием не изменяется.

## Windows onboarding — scope 033

Работа выполнена в собственной сессии 01a0b501-9a29-7b30-87a1-036ded275092, план windows-onboarding-033. Планы 031 и 032 этим выпуском не меняются. Пользователь прямо поручил собрать обе платформы и оставил все пользовательские проверки себе: не запускать VM и не использовать Computer Use. 19.09.2026 пользователь считает план завершённым с последним дополнением инструкции. Скриншот 09.53.24 подтверждает наличие Windows MCP в настройках; отдельная запись файла этим снимком не подтверждается. Новый объём продолжать через свой recovery и plan:apply; не выбирать владельца по этому справочнику.
